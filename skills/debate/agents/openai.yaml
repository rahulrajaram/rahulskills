interface:
  display_name: "Debate"
  short_description: "Run a multi-AI debate (Claude + Codex + Gemini) via gptengage."
  default_prompt: "Run $debate (or /debate where supported) to orchestrate a multi-AI debate through ~/.local/bin/gptengage debate."
