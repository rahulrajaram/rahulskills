interface:
  display_name: "Debate"
  short_description: "Run a multi-AI debate (Claude + Codex + Gemini) via gptengage."
  default_prompt: "Run $debate (or /debate where supported) through the authorized existing gptengage debate operation."
