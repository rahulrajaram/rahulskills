interface:
  display_name: "Squash Commits"
  short_description: "Safely consolidate contiguous Git commits"
  default_prompt: "Use $squash-commits to inspect recent history and propose a safe, recoverable squash plan."
