interface:
  display_name: "Markdown to PDF"
  short_description: "Convert markdown to PDF via pandoc + weasyprint."
  default_prompt: "Use $markdown-to-pdf to convert the given Markdown to PDF with the requested stylesheet and output path, preserving content without an implicit page limit."
