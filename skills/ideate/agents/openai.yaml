interface:
  display_name: "Ideate"
  short_description: "Generate divergent ideas from a seed using evolutionary ideation via gptengage."
  default_prompt: "Run $ideate (or /ideate where supported) through the authorized existing gptengage ideate operation."
