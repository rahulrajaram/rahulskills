interface:
  display_name: "Architecture Diagram"
  short_description: "Diagram architecture in the requested format; ASCII by default."
  default_prompt: "Use $archdiagram to diagram the current architecture in the requested format, defaulting to ASCII. Select deep review only when requested."
