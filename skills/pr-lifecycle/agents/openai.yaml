interface:
  display_name: "PR Lifecycle"
  short_description: "Prepare, open, and watch a GitHub PR through green CI."
  default_prompt: "Use $pr-lifecycle to prepare and open this change as a PR, perform necessary authorized checks and fixes, and follow required CI through success."
