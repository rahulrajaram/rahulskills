interface:
  display_name: "Handoff"
  short_description: "Commit changes and write a reusable NEXT_SHELL_PROMPT.md."
  default_prompt: "Use $handoff to reconcile and commit the workspace, then write the continuation prompt to NEXT_SHELL_PROMPT.md at the package root."
