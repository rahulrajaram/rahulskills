# Continuation-state fixtures

The three JSON files form a valid same-envelope continuation at the fixed
`2026-09-08T21:00:00Z` assessment time. `state_id` is the SHA-256 digest of the
state object with `state_id` removed, using the validator's canonical encoding.
